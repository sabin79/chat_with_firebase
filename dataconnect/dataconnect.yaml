specVersion: "v1beta"
serviceId: "chat_with_firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chat_with_firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
